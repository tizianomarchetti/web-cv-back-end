package com.service.cv.repository;

import com.service.cv.model.entity.TipiEnte;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipiEnteRepository extends JpaRepository<TipiEnte, Long> {
}
